/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.shutterstock.com','images.unsplash.com','source.unsplash.com'], 
      },
};

export default nextConfig;
